# Local / CI secrets — never commit the real .env
GAS_ENDPOINT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
VITE_GAS_ENDPOINT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
